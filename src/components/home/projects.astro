---
import projects from "../../collections/projects.json";
import Button from "../button.astro";
import Project from "../project.astro";
import translations from "../../locales/translations.ts";
import pathOnLanguage from "../../utils";
const currentLocale = Astro.currentLocale || 'en';
---

<section class="max-w-4xl mx-auto px-7 lg:px-0">
  <h2
    class="text-2xl font-bold leading-10 tracking-tight text-neutral-900 dark:text-neutral-100"
  >
    {translations[currentLocale].projects.title}
  </h2>
  <p class="mb-6 text-base text-neutral-600 dark:text-neutral-400">
    {translations[currentLocale].projects.description}
  </p>
  <div
    class="grid items-stretch w-full grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-7 mt-7"
  >
    {
      projects.map((project) => {
        return (
          <Project project={project} />
        )
      })
    }
  </div>

  <div class="flex items-center justify-center w-full py-5">
    <Button text={translations[currentLocale].projects.viewAll} link={pathOnLanguage("/projects", currentLocale)} />
  </div>
</section>